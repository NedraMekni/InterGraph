name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - tqdm
  - numpy
  - networkx
  - request
  - openbabel
  - rdkit
  - scipy

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - chembl_webresource_client

