name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - tqdm
  - numpy
  - networkx
  - urlib
  - request
  - pickle
  - openbabel
  - rdkit
  - scipy

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - chembl_webresource_client

